# Development guide

```bash
uv sync --frozen
uv run python scripts/quality_gate.py
```

Use `uv run python scripts/quality_gate.py --list` to discover focused checks. Use `codex --version`
for installation and configuration diagnostics.
