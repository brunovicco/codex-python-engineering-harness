# Workspace architecture

Document member ownership, public contracts, and dependency direction here. Source discovery and
default-deny package boundaries are configured under `[tool.engineering-harness.architecture]` in
the root `pyproject.toml`; the validator supports multiple globbed source roots.
